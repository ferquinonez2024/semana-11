# Ordenación de Arreglo Multidimensional
# Tarea semana 11

def bubble_sort(arr):
n = len(arr)
for i in range(n):
    for j in range(0, n-i-1):
        if arr[j] > arr[j+1]:
            arr[j], arr[j+1] = arr[j+1], arr[j]
return arr

# Crear una matriz 3x3
matriz = [[7, 6, 5], [4, 3, 2], [1, 0, -1]]

print("Matriz original:")
for fila in matriz:
print(fila)

# Ordenar la segunda fila (índice 1)
matriz[1] = bubble_sort(matriz[1])

print("\nMatriz con la segunda fila ordenada:")
for fila in matriz:
print(fila)